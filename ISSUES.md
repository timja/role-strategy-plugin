1. This text: "Changing the type will migrate the existing assignments to the selected type." is touching the radio button, it needs spacing
2. I'm not sure you need to be able to edit an assignment type once created, except when its an ambiguous type
3. When I click Assign role or Add role I'm scrolled to the bottom of the page, I wouldn't expect any scrolling to happen
4. Total count of roles missing from under search bar, see refine-ui branch
5. I still seem to get the strobe effect on assigning roles